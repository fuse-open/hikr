<Page ux:Class="HomePage">
	<ScrollView>
		<StackPanel>
			<Each Items="{hikes}">
				<Button Text="{name}" Clicked="{current.navigation.goToHike}" />
			</Each>
		</StackPanel>
	</ScrollView>
</Page>
